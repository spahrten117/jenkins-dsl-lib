#!groovy
def lib = library( "jenkins-dsl" )

try {
  stage ( 'Execute DSL Script' ) {
    node ( "${env.AGENT}" ) {
      echo "pipeline works"
    }
  }
} catch ( java.lang.Throwable error ){
  echo "This Failed"
}
